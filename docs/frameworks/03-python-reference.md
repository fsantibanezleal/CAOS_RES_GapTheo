# Python reference pipeline

Run `python data_pipeline/run.py` only when intentionally baking canonical evidence. Run `python data_pipeline/run.py --output <temporary-directory>` for a non-mutating smoke test.

Every output certificate includes method version, scenario identity, category, grouped gap data, partition residual, theorem status, dual result, star discrepancy, source trail, and SHA-256 content hash.
