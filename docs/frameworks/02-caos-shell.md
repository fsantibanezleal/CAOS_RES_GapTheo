# CAOS shell

The shared shell supplies EN/ES selection, light/dark theme, exact route navigation, the architecture modal, citations, equations, tabs, callouts, and the product footer. GapTheo supplies product content and five hand-authored SVG architecture diagrams.

The shell is a UI framework, not a mathematical dependency. Certificate truth remains inside the repository's numerical and reference engines.
