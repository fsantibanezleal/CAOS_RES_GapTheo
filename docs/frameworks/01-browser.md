# Browser framework

React state contains the scenario, selected workbench reading, and topology threshold. Derived certificates are memoized. Animations use the shared paused-visualization controller, which stops after a bounded number of points and respects user control.

The production build is relative-path safe for GitHub Pages. Direct route entry files preserve bookmarked documentation URLs.
