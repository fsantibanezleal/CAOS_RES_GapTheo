# Local development

Install browser dependencies with `npm install --prefix frontend`. Validate the full product with `npm run ci`. Start Vite with `npm run dev`.

Node 22 or later and Python 3.11 or later are supported. No environment variable, credential, database, or external service is required.
