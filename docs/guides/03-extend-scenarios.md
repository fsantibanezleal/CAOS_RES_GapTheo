# Extend the canonical matrix

Add a unique scenario to `data/input/canonical-scenarios.json` with identity, bilingual name, category, expression, anchor, and complete parameter vector. Run the intentional bake, then run `npm run ci`.

Review the generated certificate, matrix cell, hash, public replay copy, and visible case-selector category. A new case must represent a distinct scientific condition, not cosmetic duplication.
