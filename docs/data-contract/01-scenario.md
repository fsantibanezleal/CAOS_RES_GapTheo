# Scenario fields

Identity fields are `id`, `name`, `nameEs`, `category`, `expression`, and `anchor`. Mathematical fields are alpha declaration, rational numerator and denominator, point count, phase, target beta, allocator, seed, and endpoint convention.

An irrational declaration is metadata supplied by the scenario author. The software does not derive it from binary floating-point input.
