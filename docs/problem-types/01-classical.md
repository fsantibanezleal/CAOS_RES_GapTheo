# Classical irrational rotations

For a declared irrational alpha, sort the first N points of the rotation orbit and close the circle. The theorem predicts at most three distinct adjacent gap lengths; if three occur, the largest equals the sum of the smaller two.

Finite numerical agreement is a certificate for the selected state, not an algorithmic proof that a decimal input is irrational.
