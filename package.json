{
  "name": "caos-res-gaptheo",
  "version": "0.3.0",
  "private": true,
  "description": "GapTheo: a reproducible visual atlas of three-gap dynamics.",
  "scripts": {
    "build": "npm --prefix frontend run build",
    "dev": "npm --prefix frontend run dev",
    "test": "npm --prefix frontend run test",
    "typecheck": "npm --prefix frontend run typecheck",
    "generate": "python data_pipeline/run.py",
    "validate": "python scripts/validate_artifacts.py",
    "guards": "python scripts/check_content_standards.py && python scripts/check_template_residue.py && python scripts/check_product_completeness.py && python scripts/check_frontend_adr.py",
    "ci": "npm run guards && python -m unittest discover -s tests -p \"test_*.py\" && npm run typecheck && npm test && npm run build"
  }
}
