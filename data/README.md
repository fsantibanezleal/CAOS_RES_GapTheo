# Scientific data

`input/canonical-scenarios.json` is the reviewed scenario matrix. `artifacts/` and `manifests/` are deterministic outputs of the intentional reference bake. Deployment validates these files and never regenerates them.
