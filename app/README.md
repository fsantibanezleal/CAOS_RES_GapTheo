# Application surface

The shipped application lives in `frontend/`. This directory documents the deliberate absence of a separate server application. GapTheo is a static browser product with no runtime API, account, database, or secret.
