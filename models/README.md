# Method registry

GapTheo evaluates exact and numerical mathematical methods, not learned models. The generated method registry records `trainingApplicable: false` and explains this scientific boundary. No checkpoint or fabricated predictive metric belongs here.
